# SchemaInspector Syncano Socket

It is SchemaInspector integration with Syncano. It allows you to schema-inspector is an json api sanitisation and validation module.

## Endpoints

### sanitize

#### Parameters:

      schema: [object Object],
      data: '*'


### validate

#### Parameters:

      schema: [object Object],
      data: '*'

